import { type RefObject } from "react";
/**
 * Returns whether a popup should open upward: true when the space below the
 * trigger can't fit the menu (measured once the menu mounts on `open`).
 */
export declare function useDropUp(open: boolean, triggerRef: RefObject<HTMLElement | null>, menuRef: RefObject<HTMLElement | null>): boolean;
//# sourceMappingURL=useDropUp.d.ts.map